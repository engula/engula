[package]
name = "engula"
version = "0.3.0"
edition = "2021"
license = "Apache-2.0"
homepage = "https://engula.io"
repository = "https://github.com/engula/engula"
keywords = ["cache", "database"]
categories = ["caching", "database", "data-structures"]
description = "A low-latency, high-performance data structure store, used as a cache and database."

[dependencies]
clap = { version = "3.0", features = ["derive"] }
